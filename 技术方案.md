# JobRight Helper 面试任务技术方案

## 1. 目标与范围

### 1.1 目标
- 在现有浏览器扩展框架中补全 Greenhouse 表单自动填充能力。
- 基于给定 mock 数据，在两条测试链接中实现可运行的自动填充。
- 输出填充统计：成功填充字段数、未填充字段清单。

### 1.2 范围
- 支持字段类型：
  - 文本输入（`input` / `textarea`）
  - 下拉选择（`select`）
  - 教育经历列表（可新增多条并填充子字段）
- 支持页面：
  - `job-boards.greenhouse.io/embed/job_app?...&jr_id=69fa3805...`
  - `job-boards.greenhouse.io/...?...&gh_jid=4599649006...`

### 1.3 非目标
- 不做通用 ATS 平台适配（仅 Greenhouse）。
- 不做复杂 AI 语义映射，仅使用规则匹配 + 简单字符串归一化。
- 不实现自动提交（只填充，不点击 Submit）。

## 2. 现状分析（基于现有代码）

- 入口已具备：`contents/index.tsx` 提供 `Auto Fill Form` 按钮并调用 `autofillInstance.fillForm()`。
- 平台识别已具备：`contents/crawler/factory.ts` 能识别 Greenhouse 域名与 `gh_jid` 参数。
- 执行器已具备：`contents/crawler/utils/executor.ts` 支持串行执行和步骤间延迟。
- 输入工具已具备：
  - `fillDefaultInputField`：文本输入事件链触发完整。
  - `fillCheckbox`：复选框点击能力已存在。
  - `findMatchOption`：下拉选项模糊匹配能力已存在。
- 核心待实现集中在 `contents/crawler/greenhouse.ts`：
  - `extractFields`
  - `fillForm` 内数据映射与 action 组装
  - `getFormElementExecutor`
  - `handleFilledInfo`
  - `fillInputTextField` / `fillSelectField` / `fillEducation`

## 3. 方案总览

采用“四阶段流水线”：

1. **页面字段提取（Extract）**  
   扫描 DOM，产出标准化字段规则 `formRules`。
2. **mock 数据标准化（Normalize）**  
   将不同结构（`name/value`、`Education`）统一为可检索映射。
3. **动作生成与串行执行（Plan & Execute）**  
   为每个字段生成执行器，按顺序填充并触发事件。
4. **结果统计与日志输出（Report）**  
   输出填充成功/失败字段及数量，便于验收。

## 4. 关键设计

## 4.1 字段提取设计（extractFields）

### 扫描策略
- 按 `label` 或字段容器扫描表单区块，过滤隐藏/无效标签（复用 `shouldSkipLabel`）。
- 对每个候选字段识别类型：
  - `input[type=text|email|tel|url]`、`textarea` -> `text`
  - `select` -> `select`
  - 教育区块（根据标题或容器特征）-> `education`

### 产出结构
建议 `TRule` 扩展为包含定位信息的结构（若不改类型，可在类内额外 map）：
- `label`: 字段标签文本（去特殊字符）
- `type`: `text | select | education`
- `options`: 下拉选项列表（仅 select）
- `elementRef`: 对应 DOM 元素引用（运行期对象，不必持久化）

### 日志要求
- `console.table(formRules)` 打印提取结果，满足“爬取所有表单项并打印”的要求。

## 4.2 mock 数据标准化设计

### 数据特点
- 普通字段：`{ name: string, value: string | string[] }`
- 教育字段：`{ Education: EducationItem[] }`

### 标准化结果
- `fieldAnswerMap: Map<string, string | string[]>`
  - key：归一化后字段名（小写、去标点、压缩空格）
  - value：答案
- `educationAnswerList: Education[]`
  - 由 `Education` 专项提取

### 链接与 mock 的映射
- 通过 URL 参数解析：
  - 优先 `gh_jid`
  - 其次 `jr_id`（当前题目可写死映射到 `5123089007.json`）
- 本地开发建议：
  - 先在 `greenhouse.ts` 内部写 `MOCK_DATA_MAP`
  - 后续可扩展为远端拉取或配置化映射

## 4.3 字段匹配策略

### 标签归一化
- 去除 `*`、`✱`、冒号、括号等噪音符号。
- 转小写并去多余空格。

### 匹配优先级
1. **精确匹配**：`normalized(label) === normalized(name)`
2. **包含匹配**：双向 `includes`
3. **同义词映射（可选增强）**：如 `phone number` -> `phone`

### 选项匹配
- 对 `select` 与 `radio/checkbox`（若页面出现）复用 `findMatchOption`。
- mock 值为数组时，优先取首值（本题单选场景足够）。

## 4.4 自动填充执行设计

### 统一执行模型
- `getFormElementExecutor(rule)` 返回 `ExecutableFunction[]`：
  - 每个函数只负责一个字段填充动作。
  - 由 `executeSequentially` 串行调度，避免页面联动冲突。

### 文本填充
- `fillInputTextField(element, value)`：
  - 调用 `fillDefaultInputField`
  - 保证触发 `input/change/blur` 事件链

### 下拉填充
- `fillSelectField(element, value)`：
  - 遍历 `option`，使用归一化比较/模糊匹配定位目标
  - 设置 `selectedIndex` + 触发 `change`

### 教育列表填充
- `fillEducation()`：
  1. 根据页面按钮（如 “Add Education”）逐条新增条目
  2. 对每条教育数据填充子字段：School、Degree、Discipline、Start、End
  3. 当前条填完再处理下一条，避免索引错位

## 4.5 统计与可观测性设计（handleFilledInfo）

维护三类集合：
- `allFieldLabels`: 页面可填字段全集
- `filledFieldLabels`: 成功执行并确认值写入字段
- `unfilledFieldLabels`: 存在字段但无匹配答案或填充失败

输出建议：
- `filledCount = filledFieldLabels.length`
- `unfilledCount = unfilledFieldLabels.length`
- `console.info("[AutoFill] filled:", filledFieldLabels)`
- `console.warn("[AutoFill] unfilled:", unfilledFieldLabels)`
- `console.table({ filledCount, unfilledCount, total: allFieldLabels.length })`

## 5. 实施步骤（落地到代码）

1. 在 `greenhouse.ts` 实现 URL -> mock 数据读取逻辑。
2. 完成 `extractFields`：
   - 扫描标签与元素
   - 构建 `formRules`
   - 打印字段清单
3. 完成 `fillForm`：
   - 拉取并标准化 mock 数据
   - 生成动作队列并串行执行
4. 完成 `getFormElementExecutor`：
   - 按 `rule.type` 分派到不同填充函数
5. 完成 `fillInputTextField`、`fillSelectField`、`fillEducation`。
6. 完成 `handleFilledInfo` 统计输出。
7. 在两条测试链接进行联调，修正 edge case。

## 6. 验收标准与测试计划

### 功能验收
- 点击 `Auto Fill Form` 后能自动填入主要字段（姓名/邮箱/电话/问答/教育）。
- 控制台能打印：
  - 提取到的字段列表
  - 成功填充字段数
  - 未填充字段列表

### 回归测试用例
- 用例1：`jr_id=69fa3805...` 页面 + `mocks/5123089007.json`
- 用例2：`gh_jid=4599649006` 页面 + `mocks/4599649006.json`
- 校验点：
  - 文本框正确写值
  - 下拉框正确选项
  - 教育至少两条可填充
  - 无阻塞性报错

### 交付物对齐
- 代码实现（核心在 `greenhouse.ts`）
- 5-10 秒演示录屏（点击按钮 -> 自动填充 -> 控制台统计）

## 7. 风险与应对

- **风险1：页面结构变体多**  
  - 应对：规则优先 + 容器回退策略，不依赖单一选择器。
- **风险2：字段文案不完全一致**  
  - 应对：标签归一化 + 包含匹配 + 小规模别名字典。
- **风险3：联动渲染导致填充时序问题**  
  - 应对：串行执行并保留延迟，必要时在关键步骤加短暂等待。
- **风险4：教育列表索引错位**  
  - 应对：每次新增后重新查询当前条容器，不缓存旧节点引用。

## 8. 预计工时

- 字段提取与规则结构：1.5h
- mock 映射与填充执行：2h
- 教育列表处理：1.5h
- 统计与日志、联调修复：1h
- 录屏与整理交付：0.5h
- **总计：约 6.5 小时**

